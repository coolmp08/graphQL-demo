type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
    ISBN: String
}

type Author {
    id: ID
    firstName: String
    lastName: String
}